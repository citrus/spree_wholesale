class WholesalerTest < ActiveSupport::TestCase

  test "user can have wholesaler" do
    
    
    #order = Order.new
    #assert order.respond_to?(:wholesale), "Order is not wholesale ready. Try running `rake db:migrate` first."
    
  end
    
end
